server:
  port: 8081

spring:
  security:
    user:
      name: bootcampRoot
      password: bootcampRoot123
  cloud:
    config:
      server:
        git:
          uri: https://github.com/wrosado24/proyx.git
          search-paths: config-repository
          default-label: "master"
